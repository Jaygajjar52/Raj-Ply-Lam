# Base URL of the backend API.
# Local dev can stay empty because Vite proxies /api to localhost:4000.
# Production must be your deployed backend URL, for example:
# VITE_API_BASE_URL=https://raj-ply-lam-api.onrender.com/api
VITE_API_BASE_URL=
