# Backend API base URL.
# Local dev can use /api because Vite proxies it to http://localhost:4000.
VITE_API_BASE_URL="/api"

# Production must point to the deployed backend and end with /api.
# VITE_API_BASE_URL="https://your-backend-host.com/api"
